# audio_override.yml — generated by scripts/build_podcast.rb.
# Maps post.slug → {url, bytes} for posts that set
# `audio_override:` in frontmatter. Used by
# `_layouts/podcast-rss.xml` to populate <enclosure length="…">
# so podcast clients accept the items.
--- {}
